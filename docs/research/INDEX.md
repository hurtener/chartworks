# Research briefs — reverse index

> Subsystem → informing briefs. A phase plan cites the briefs it inherits from
> ("Brief findings incorporated" section); a plan citing no brief is a drift signal
> (CLAUDE.md §16). Briefs are authoritative for *context*, never for design — the RFC
> and phase plans decide.

Empty — phase-0 research populates this table (00_KICKSTART-PROMPT.md, Phase B). One
brief is mandatory regardless of scope: the **predecessor-diff brief** — the client
predecessor (`_ref/original_wayfinder`) vs the generalistic fork
(`_ref/forked_wayfinder_explorer`), covering every divergence worth carrying,
topic lifecycle exhaustively. Both predecessors are referred to only by those two
names (CLAUDE.md "Predecessor hygiene"); no predecessor code or file is ever pasted
into a brief.

## Briefs

| # | File | Subject |
|---|------|---------|
| 05 | `05-predecessor-diff.md` | *(mandatory)* Client predecessor vs. generalistic fork: every divergence worth carrying, topic lifecycle exhaustively |

## Subsystem → briefs

<!-- Populated as phase plans are authored and cite their informing briefs
     (CLAUDE.md §16 step 3). -->

| Subsystem / phase area | Primary briefs | Secondary |
|---|---|---|
| | | |
